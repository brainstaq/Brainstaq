<div class="container pt-5 pb-5">
  <h3 class="mt-1 mb-3" style="font-size: 18px; font-weight: bold;">
		Fixed Costs
	</h3>
  <div class="row">
    <div class="col-12">
      <table class="table table-bordered table-hover">
        <thead>
          <tr>
            <th scope="col">Item</th>
            <th scope="col">Cost</th>
          </tr>
        </thead>
				<tbody>
					<% @business_plan.fixed_costs.each do |fixed_cost| %>
						<tr>
							<td><%= fixed_cost.item %></td>
							<td><%= number_to_currency(fixed_cost.cost, :unit => "₦") %></td>
						</tr>
	        <% end %>
				</tbody>
        <tr>
          <td class="mb-3" style="font-size: 18px; font-weight: bold;"> Total Fixed Costs: </td>
          <td class="mb-3" style="font-size: 18px; font-weight: bold;">
            <%= number_to_currency(@fixed_costs.sum(:cost), :unit => "₦") %>
          </td>
        </tr>
      </table>
    </div>
  </div>
</div>