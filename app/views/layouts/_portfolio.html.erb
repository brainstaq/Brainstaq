<section class="section-portfolio">
	<div class="container">
		<!-- Start Header Section -->
		<div class="row justify-content-center text-center">
			<div class="col-md-8 col-lg-6">
			</div>
		</div>
		<!-- / End Header Section -->

		<% if user_signed_in? && current_user.id == @enterprise.user_id %>
			<%= link_to fa_icon("edit", title: "Add Service", class: "text-primary float-right mr-2 mt-2", style: "font-size: 20px;"), 
			new_enterprise_portfolio_path(@enterprise) %>
		<% end %>
			
    	<div class="row justify-content-center mt-5 text-center">
      	<% @enterprise.portfolios.each do |portfolio| %>
					<!-- Start Single Person -->
					<div class="col-sm-4 mb-5">
          <% if user_signed_in? && current_user.id == @enterprise.user_id %>
						<div class="dropdown mr-3 mt-5 float-right" style="font-size: 20px;">
							<a class="dropdown-toggle fas fa-ellipsis-v" href="#" role="button" id="dropdownMenuLink" 
								data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
							</a>
							<div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink" style="font-size: 12px;">
								<%= link_to "Edit", edit_enterprise_portfolio_path(@enterprise, portfolio), class: "dropdown-item" %>
								<%= link_to "Delete", [@enterprise, portfolio], :method => 'delete', data: { confirm: 'Are you sure?' }, class: "dropdown-item" %>
							</div>
						</div>
          <% end %>
						<div class="single-person">
							<div class="person-image">
								<%= image_tag portfolio.image.url, skip_pipeline: true %>
							</div>
							<div>
								<h5 class="full-name"><%= portfolio.title %></h5>
                <span class="mt-0" style="font-size: 12px;">-- <%= portfolio.location %> --</span>
								<h6 class=""><small><%= sanitize portfolio.description %></small></h6>
							</div>
						</div>
					</div><!-- / End Single Person -->
				<% end %>
      </div>
		</div>
	</section>

  <style>
p,a,h1,h2,h3,h4 {
	margin: 0;
	padding: 0;
}

.section-portfolio .header-section {
	margin-bottom: 50px;
}

.section-portfolio .header-section .title {
    font-weight: 700;
    font-size: 45px;
		color: #000;
}

.section-portfolio .single-person {
	margin-top: 20px;
	padding: 50px;
	background-color: #f6f9ff;
	/* border-radius: 5px; */
}

.section-portfolio .single-person .person-image {
    position: relative;
    margin-bottom: 10px;
    /* border-radius: 5px; */
    transition: padding .3s;
}

.section-portfolio .single-person:hover .person-image {
	padding: 10px;
  border: 1px solid gray;
}

.section-portfolio .single-person .person-image img {
	width: 100%;
  height: 100%;
  /* border-radius: 5px !important; */
}

.full-name {
	margin-bottom: 1px;
	font-size: 16px;
}

.section-portfolio .single-person:hover .full-name {
	text-transform: uppercase;
}

  </style>