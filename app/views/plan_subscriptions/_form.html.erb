<%= form_with(model: plan_subscription) do |form| %>
  <% if plan_subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plan_subscription.errors.count, "error") %> prohibited this plan_subscription from being saved:</h2>

      <ul>
        <% plan_subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
